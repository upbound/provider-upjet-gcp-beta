---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: repositories.dataform.gcp-beta.upbound.io
spec:
  group: dataform.gcp-beta.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - gcp-beta
    kind: Repository
    listKind: RepositoryList
    plural: repositories
    singular: repository
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: Repository is the Schema for the Repositorys API. A resource
          represents a Dataform Git repository
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RepositorySpec defines the desired state of Repository
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  deletionPolicy:
                    description: 'Policy to control how the repository and its child
                      resources are deleted. When set to FORCE, any child resources
                      of this repository will also be deleted. Possible values: DELETE,
                      FORCE. Defaults to DELETE.'
                    type: string
                  displayName:
                    description: Optional. The repository's user-friendly name.
                    type: string
                  gitRemoteSettings:
                    description: |-
                      Optional. If set, configures this repository to be linked to a Git remote.
                      Structure is documented below.
                    properties:
                      authenticationTokenSecretVersion:
                        description: The name of the Secret Manager secret version
                          to use as an authentication token for Git operations. This
                          secret is for assigning with HTTPS only(for SSH use ssh_authentication_config).
                          Must be in the format projects//secrets//versions/*.
                        type: string
                      defaultBranch:
                        description: The Git remote's default branch name.
                        type: string
                      sshAuthenticationConfig:
                        description: |-
                          Authentication fields for remote uris using SSH protocol.
                          Structure is documented below.
                        properties:
                          hostPublicKey:
                            description: Content of a public SSH key to verify an
                              identity of a remote Git host.
                            type: string
                          userPrivateKeySecretVersion:
                            description: The name of the Secret Manager secret version
                              to use as a ssh private key for Git operations. Must
                              be in the format projects//secrets//versions/*.
                            type: string
                        type: object
                      url:
                        description: The Git remote's URL.
                        type: string
                    type: object
                  kmsKeyName:
                    description: |-
                      Optional. The reference to a KMS encryption key. If provided, it will be used to encrypt user data in the repository and all child resources.
                      It is not possible to add or update the encryption key after the repository is created. Example projects/[kms_project_id]/locations/[region]/keyRings/[key_region]/cryptoKeys/[key]
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      Optional. Repository user labels.
                      An object containing a list of "key": value pairs. Example: { "name": "wrench", "mass": "1.3kg", "count": "3" }.
                    type: object
                    x-kubernetes-map-type: granular
                  npmrcEnvironmentVariablesSecretVersion:
                    description: Optional. The name of the Secret Manager secret version
                      to be used to interpolate variables into the .npmrc file for
                      package installation operations. Must be in the format projects//secrets//versions/*.
                      The file itself must be in a JSON format.
                    type: string
                  project:
                    description: |-
                      The ID of the project in which the resource belongs.
                      If it is not provided, the provider project is used.
                    type: string
                  region:
                    description: A reference to the region
                    type: string
                  serviceAccount:
                    description: The service account to run workflow invocations under.
                    type: string
                  workspaceCompilationOverrides:
                    description: |-
                      If set, fields of workspaceCompilationOverrides override the default compilation settings that are specified in dataform.json when creating workspace-scoped compilation results.
                      Structure is documented below.
                    properties:
                      defaultDatabase:
                        description: The default database (Google Cloud project ID).
                        type: string
                      schemaSuffix:
                        description: The suffix that should be appended to all schema
                          (BigQuery dataset ID) names.
                        type: string
                      tablePrefix:
                        description: The prefix that should be prepended to all table
                          names.
                        type: string
                    type: object
                required:
                - region
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  deletionPolicy:
                    description: 'Policy to control how the repository and its child
                      resources are deleted. When set to FORCE, any child resources
                      of this repository will also be deleted. Possible values: DELETE,
                      FORCE. Defaults to DELETE.'
                    type: string
                  displayName:
                    description: Optional. The repository's user-friendly name.
                    type: string
                  gitRemoteSettings:
                    description: |-
                      Optional. If set, configures this repository to be linked to a Git remote.
                      Structure is documented below.
                    properties:
                      authenticationTokenSecretVersion:
                        description: The name of the Secret Manager secret version
                          to use as an authentication token for Git operations. This
                          secret is for assigning with HTTPS only(for SSH use ssh_authentication_config).
                          Must be in the format projects//secrets//versions/*.
                        type: string
                      defaultBranch:
                        description: The Git remote's default branch name.
                        type: string
                      sshAuthenticationConfig:
                        description: |-
                          Authentication fields for remote uris using SSH protocol.
                          Structure is documented below.
                        properties:
                          hostPublicKey:
                            description: Content of a public SSH key to verify an
                              identity of a remote Git host.
                            type: string
                          userPrivateKeySecretVersion:
                            description: The name of the Secret Manager secret version
                              to use as a ssh private key for Git operations. Must
                              be in the format projects//secrets//versions/*.
                            type: string
                        type: object
                      url:
                        description: The Git remote's URL.
                        type: string
                    type: object
                  kmsKeyName:
                    description: |-
                      Optional. The reference to a KMS encryption key. If provided, it will be used to encrypt user data in the repository and all child resources.
                      It is not possible to add or update the encryption key after the repository is created. Example projects/[kms_project_id]/locations/[region]/keyRings/[key_region]/cryptoKeys/[key]
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      Optional. Repository user labels.
                      An object containing a list of "key": value pairs. Example: { "name": "wrench", "mass": "1.3kg", "count": "3" }.
                    type: object
                    x-kubernetes-map-type: granular
                  npmrcEnvironmentVariablesSecretVersion:
                    description: Optional. The name of the Secret Manager secret version
                      to be used to interpolate variables into the .npmrc file for
                      package installation operations. Must be in the format projects//secrets//versions/*.
                      The file itself must be in a JSON format.
                    type: string
                  project:
                    description: |-
                      The ID of the project in which the resource belongs.
                      If it is not provided, the provider project is used.
                    type: string
                  serviceAccount:
                    description: The service account to run workflow invocations under.
                    type: string
                  workspaceCompilationOverrides:
                    description: |-
                      If set, fields of workspaceCompilationOverrides override the default compilation settings that are specified in dataform.json when creating workspace-scoped compilation results.
                      Structure is documented below.
                    properties:
                      defaultDatabase:
                        description: The default database (Google Cloud project ID).
                        type: string
                      schemaSuffix:
                        description: The suffix that should be appended to all schema
                          (BigQuery dataset ID) names.
                        type: string
                      tablePrefix:
                        description: The prefix that should be prepended to all table
                          names.
                        type: string
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: RepositoryStatus defines the observed state of Repository.
            properties:
              atProvider:
                properties:
                  deletionPolicy:
                    description: 'Policy to control how the repository and its child
                      resources are deleted. When set to FORCE, any child resources
                      of this repository will also be deleted. Possible values: DELETE,
                      FORCE. Defaults to DELETE.'
                    type: string
                  displayName:
                    description: Optional. The repository's user-friendly name.
                    type: string
                  effectiveLabels:
                    additionalProperties:
                      type: string
                    description: for all of the labels present on the resource.
                    type: object
                    x-kubernetes-map-type: granular
                  gitRemoteSettings:
                    description: |-
                      Optional. If set, configures this repository to be linked to a Git remote.
                      Structure is documented below.
                    properties:
                      authenticationTokenSecretVersion:
                        description: The name of the Secret Manager secret version
                          to use as an authentication token for Git operations. This
                          secret is for assigning with HTTPS only(for SSH use ssh_authentication_config).
                          Must be in the format projects//secrets//versions/*.
                        type: string
                      defaultBranch:
                        description: The Git remote's default branch name.
                        type: string
                      sshAuthenticationConfig:
                        description: |-
                          Authentication fields for remote uris using SSH protocol.
                          Structure is documented below.
                        properties:
                          hostPublicKey:
                            description: Content of a public SSH key to verify an
                              identity of a remote Git host.
                            type: string
                          userPrivateKeySecretVersion:
                            description: The name of the Secret Manager secret version
                              to use as a ssh private key for Git operations. Must
                              be in the format projects//secrets//versions/*.
                            type: string
                        type: object
                      tokenStatus:
                        description: |-
                          (Output)
                          Indicates the status of the Git access token. https://cloud.google.com/dataform/reference/rest/v1beta1/projects.locations.repositories#TokenStatus
                        type: string
                      url:
                        description: The Git remote's URL.
                        type: string
                    type: object
                  id:
                    description: an identifier for the resource with format projects/{{project}}/locations/{{region}}/repositories/{{name}}
                    type: string
                  kmsKeyName:
                    description: |-
                      Optional. The reference to a KMS encryption key. If provided, it will be used to encrypt user data in the repository and all child resources.
                      It is not possible to add or update the encryption key after the repository is created. Example projects/[kms_project_id]/locations/[region]/keyRings/[key_region]/cryptoKeys/[key]
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      Optional. Repository user labels.
                      An object containing a list of "key": value pairs. Example: { "name": "wrench", "mass": "1.3kg", "count": "3" }.
                    type: object
                    x-kubernetes-map-type: granular
                  npmrcEnvironmentVariablesSecretVersion:
                    description: Optional. The name of the Secret Manager secret version
                      to be used to interpolate variables into the .npmrc file for
                      package installation operations. Must be in the format projects//secrets//versions/*.
                      The file itself must be in a JSON format.
                    type: string
                  project:
                    description: |-
                      The ID of the project in which the resource belongs.
                      If it is not provided, the provider project is used.
                    type: string
                  region:
                    description: A reference to the region
                    type: string
                  serviceAccount:
                    description: The service account to run workflow invocations under.
                    type: string
                  terraformLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      The combination of labels configured directly on the resource
                      and default labels configured on the provider.
                    type: object
                    x-kubernetes-map-type: granular
                  workspaceCompilationOverrides:
                    description: |-
                      If set, fields of workspaceCompilationOverrides override the default compilation settings that are specified in dataform.json when creating workspace-scoped compilation results.
                      Structure is documented below.
                    properties:
                      defaultDatabase:
                        description: The default database (Google Cloud project ID).
                        type: string
                      schemaSuffix:
                        description: The suffix that should be appended to all schema
                          (BigQuery dataset ID) names.
                        type: string
                      tablePrefix:
                        description: The prefix that should be prepended to all table
                          names.
                        type: string
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
